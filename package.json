{
  "version": "0.0.1",
  "scripts": {
    "docs:dev": "set NODE_OPTIONS=--openssl-legacy-provider && vuepress dev",
    "docs:build": "set NODE_OPTIONS=--openssl-legacy-provider && vuepress build"
  },
  "devDependencies": {
    "@vuepress/plugin-pwa": "^1.9.9",
    "vuepress": "^1.9.9"
  },
  "dependencies": {
    "@vuepress/core": "^1.9.9"
  }
}